---
name: fitness-assistant
slug: fitness-assistant
displayName: 健身助手
description: 健身比价助手——私教/月卡/器械，算清单次真实成本。当用户想比健身价格、健身房年卡值不值、私教课多少钱、健身卡比价、健身器械价格、fitness price comparison 时使用。覆盖：美团本地生活（健身房/私教/团课）、大众点评、京东（健身器械）。课程卡折算单次价对比，私教课包/团课/月卡横向比，器械规格参数对比。需城市与健身需求信息。只做比价，不代下单、不代付款。
summary: "健身比价助手——私教/月卡/器械，算清单次真实成本。当用户想比健身价格、健身房年卡值不值、私教课多少钱、健身卡比价、健身…"
version: 1.1.0
license: MIT
tags: 健身比价,健身房,私教,健身卡,年卡,月卡,团课,健身器械,大众点评,美团,fitness-comparison,健身助手
---

# 健身助手

私教课/健身卡/器械，算清**单次真实成本**再决策。

## 工作流

1. **解析需求**：提取 `城市`、`需求`（健身卡/私教课/团课/器械）、`周期`（月卡/季卡/年卡）、可选 `区域/健身房名`、`预算`。缺城市或需求时先问一次。

2. **平台路由**：课程卡/健身房默认美团本地生活 → 大众点评；器械走京东/淘宝。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

3. **逐平台采集**：搜索健身房/课程 → 读前 5~10 条。每条记录至少含：`平台`、`健身房/商家`、`产品`（年卡/月卡/私教包）、`价格`、`包含内容`（次数/时长/是否含器械区）、`评分`、`链接`。

4. **登录/定位移交**：登录墙、验证码、定位墙出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

5. **口径归一（读 `references/fitness-assistant-knowledge.md`）**：
   - **单次价折算**：年卡价 ÷ 预估使用次数、私教包 ÷ 课时数，折算单次/单课时成本再比；
   - **班型区分**：1 对 1 私教 vs 团课 vs 自主训练，单价差异大，不混比；
   - **器械按型号**：跑步机/椭圆机/哑铃组按型号与参数比，不按品牌笼统比。

6. **报告输出**：Markdown 表 `平台 | 商家 | 产品 | 价格 | 单次/单课时价 | 包含内容 | 评分 | 链接 | 备注`，结论给出"最低单次成本路径 + 使用频率提醒"。**每个价格必须附可点击的购买链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为内部实现（复用主技能 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者明示）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单产品 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代下单、不代付款；价格页面实读，禁止编造。

## 边界声明

- 健身卡是否划算取决于使用频率，报告按"每周使用 X 次"折算单次价，提醒用户按自身频率判断。
- 私教课有效期、请假/转让规则各店不同，报告提示查看合同条款。
- 器械比价只列规格参数，不构成选购建议；大件器械注意安装与售后费用。
